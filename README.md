# BLACKSITE

Browser-based tactical FPS prototype with Three.js.

## Deploy
1. Host static files on a web server.
2. Use `/var/www/blacksite` for VPS deployment.
3. Prefer subdomain `blacksite.powrsport.com` or separate path.
